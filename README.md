# BookStore

Angular , ngrx sample test project 

## Build and Running

 - Install package manager  -  `$ npm install` 
 - Run front end  -  open command prompt and run `$ ng serve`  . Navigate to `http://localhost:4200/`.
 - Run back end server -  open new command prompt and run `$ json-server --watch db.json` 
